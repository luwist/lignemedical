import { NgTooltipDirective } from './ng-tooltip.directive';

describe('NgTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
