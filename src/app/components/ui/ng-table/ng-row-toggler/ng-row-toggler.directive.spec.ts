import { NgRowTogglerDirective } from './ng-row-toggler.directive';

describe('NgRowTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgRowTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
