import { NgStepperTriggerDirective } from './ng-stepper-trigger.directive';

describe('NgStepperTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new NgStepperTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
