import { NgStepperStepIndexDirective } from './ng-stepper-step-index.directive';

describe('NgStepperStepIndexDirective', () => {
  it('should create an instance', () => {
    const directive = new NgStepperStepIndexDirective();
    expect(directive).toBeTruthy();
  });
});
